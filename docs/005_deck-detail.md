# 005 デッキ詳細（/decks/[id]）

デッキ情報の表示、カード一覧、復習開始ボタン。
004 完了後に着手する。

---

## 関連ファイル

- `app/decks/[id]/page.tsx`（新規）
- `app/decks/[id]/loading.tsx`（新規）
- `app/decks/[id]/not-found.tsx`（新規）
- `components/CardList.tsx`（新規）

---

## TODO

### デッキ詳細ページ（app/decks/[id]/page.tsx）
- [x] `params` を `await` して `id` を取得する（Next.js 15以降の型）
- [x] Server Component でデッキ情報を取得し、存在しない場合は `notFound()` を呼ぶ
- [x] デッキ名・説明・言語・公開設定を表示する
- [x] そのデッキのカード一覧を取得して表示する（`Promise.all` で並列取得）
- [x] 「復習開始」ボタンを設置する（`/decks/[id]/study` へ遷移）
- [x] 「カード追加」ボタンを設置する（`/decks/[id]/cards/new` へ遷移）
- [x] 自分のデッキの場合のみ編集・削除ボタンを表示する

### CardList コンポーネント
- [x] カードの単語・読み方・意味・品詞・レベルを一覧表示する
- [x] カードがない場合は空状態メッセージを表示する

### not-found.tsx
- [x] デッキが存在しない場合の404ページを実装する

### loading.tsx
- [x] スケルトンUIを実装する

### 動作確認
- [x] 自分のデッキにアクセスできることを確認する
- [x] 他ユーザーの公開デッキにアクセスできることを確認する
- [x] 他ユーザーの非公開デッキにアクセスすると404になることを確認する
- [x] 存在しないIDにアクセスすると not-found が表示されることを確認する
